# Integration tests run against assembled subsystems (engine + domain + plugin).
# Real tests land alongside their features.
add_custom_target(noted_integration_tests_placeholder
    COMMENT "integration tests: placeholder — real targets land per-feature")
